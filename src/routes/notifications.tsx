import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { AppShell, Card } from "@/components/app-shell";
import { useAuth } from "@/lib/auth";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/notifications")({component:Notifications});
function Notifications(){const {user}=useAuth();const nav=useNavigate();const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{if(!user){setLoading(false);return}setLoading(true);supabase.from("notifications").select("*").eq("user_id",user.id).order("created_at",{ascending:false}).limit(50).then(({data})=>{setItems(data??[]);setLoading(false)})},[user]);const open=async(n:any)=>{if(!n.read){await supabase.from("notifications").update({read:true}).eq("id",n.id);setItems(x=>x.map(item=>item.id===n.id?{...item,read:true}:item))}if(n.challenge_id)nav({to:"/challenge/$id",params:{id:n.challenge_id}})};return <AppShell title="Notifications"><div className="mx-auto max-w-3xl space-y-2">{loading?<p className="text-sm text-muted-foreground">Loading notifications…</p>:items.map(n=><button key={n.id} onClick={()=>open(n)} className={`block w-full text-left ${n.read?"opacity-60":""}`}><Card className="hover:bg-surface"><p className="text-sm font-semibold">{n.type.replaceAll("_"," ")}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(n.created_at).toLocaleString()}</p></Card></button>)}{!loading&&items.length===0&&<Card><p className="text-sm text-muted-foreground">You're all caught up. Notifications will appear here.</p></Card>}</div></AppShell>}
